# Runtime dependencies mirror [project.dependencies] in pyproject.toml, which is
# the source of truth. Install the project instead of this file where possible:
#   pip install -e .
# A fully pinned lockfile (e.g. uv.lock or pip-tools output) is produced in Milestone 0.
-e .
